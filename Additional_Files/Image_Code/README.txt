The core.py file provided is for the hips2fits service to increase the timeout.

The location it needs to be placed at is as follows;

C:/users/user/anaconda3/Lib/site-packages/astroquery/hips2fits

Then override the existing file, and the timeout is then increased, and the code will work.